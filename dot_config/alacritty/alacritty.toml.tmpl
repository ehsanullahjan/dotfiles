# https://alacritty.org/config-alacritty.html

[general]
import = [
  "~/.config/alacritty/theme.toml",
]

[env]
TERM = {{ .terminal_type | quote }}

[window]
startup_mode = "maximized"
# startup_mode = "windowed"
# dimensions = {
#   columns = 116,
#   lines = 38,
# }

decorations = "full"

padding = {x = 4, y = 4}
dynamic_padding = true

option_as_alt = "both"

[scrolling]
history = {{ .terminal_scrollback_limit }}
multiplier = 3

[font]
normal = { family = {{ .font_mono_family | quote }} }
size = {{ .font_mono_size }}
# offset = { x = 0, y = 0 }
# glyph_offset = { x = 0, y = 0 }

[bell]
duration = 0

[selection]
save_to_clipboard = true

[cursor]
style = {
    shape = "beam",
    blinking = "off",
}

vi_mode_style = {
    shape = "block",
    blinking = "off",
}

unfocused_hollow = true

[terminal]
shell = { program = {{ .shell_path | quote }} }

[mouse]
hide_when_typing = true
bindings = [
  { mouse = "Middle", action = "Paste" },
]

[keyboard]
bindings = [
    { key = "F11", action = "ToggleFullscreen" },
    { key = "N", mods = "Control|Shift", action = "CreateNewWindow" },
]
